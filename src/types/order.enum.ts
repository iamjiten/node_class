export enum OrderStatus {
  PENDING = "pending",
  SHIPPING = "shipping",
  PROCESSING = "processing",
  DELIVERED = "delivered",
}

export enum OrderItemStatus {
  PENDING = "pending",
  PROCESSING = "processing",
  DELIVERED = "delivered",
}
